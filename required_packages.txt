# Direct dependencies only. Transitive dependencies are resolved by pip
# per platform - do not pin them here or re-freeze this file with
# `pip freeze`, which captures a platform-specific transitive closure
# (e.g. manylinux-only wheels) that breaks installs on other platforms.
#
# jupyter is intentionally not listed here: the README's
# `conda create -n sat python=3.9.21 pip jupyter` step already
# provides it.
#
# tensorflow-text is NOT listed here. It is only needed to load the
# multilingual USE v3 encoder used by the Spanish-language chile_xlxs/
# chile_csv configs in processing/pipeline.py - the CCP constitutions
# and anarchism-document configs use the English 'use-4' encoder and
# do not need it. tensorflow-text has no Windows wheel compatible with
# TensorFlow 2.17, so it is not a base requirement; if you need the
# Spanish-language configs on Linux or macOS, additionally run:
#   pip install tensorflow-text==2.17.0
ipywidgets==8.1.5
lxml==5.3.0
matplotlib==3.9.4
matplotlib-venn==0.11.10
networkx==3.2.1
numpy==1.26.2
openpyxl==3.1.5
pandas==2.2.3
scipy==1.13.1
spacy==3.8.7
tensorflow==2.17.0
tensorflow-hub==0.16.1
textract==2.0.0
